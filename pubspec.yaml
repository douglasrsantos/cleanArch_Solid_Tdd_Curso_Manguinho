name: fordev
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.11
  faker: ^2.1.0
  flutter_lints: ^3.0.0
  mockito: ^5.4.4
  test: ^1.25.2

flutter:
  uses-material-design: true
